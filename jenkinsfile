pipeline {
    agent {
        label 'windows' 
    }

    stages {
        stage('Create Directory') {
            steps {
                bat 'mkdir demo'
            }
        }

        stage('List Directory') {
            steps {
                bat 'msbuild'
            }
        }
    }
}
